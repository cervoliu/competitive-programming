{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "Launch Program",
            "program": "${fileDirname}/${fileBasenameNoExtension}.elf",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "Compile",
        }
    ]
}